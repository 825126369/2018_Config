Pthread线程测试结果分析：
(1)建立主程序方法，线程1方法，线程2方法。分别读同一个全局变量int，不会产生资源占用的情况（就如同无数的男人都可以用眼看同一个漂亮的女人）
(2)同上条件。如果分别去访问这个资源，且用于尝试改变这个int的值的话，就会造成所谓“资源占用的情况，无法访问到资源”。
(3)同上条件，把int改为数组，一起读没有问题，如果 分别尝试改变数组的状态或改变其元素的状态就会导致资源被占用的情况，这样会导致程序跳过这步执行，甚至导致程序无法继续执行下去。
